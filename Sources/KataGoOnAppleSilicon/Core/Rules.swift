// Stub for Go rules (Chinese rules)
public struct Rules {
    // Placeholder: ko, suicide, scoring
    
    public enum KoRule {
        case simple
        case positional
        case situational
    }
    
    public enum ScoringRule {
        case area
        case territory
    }
}